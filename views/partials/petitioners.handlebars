{{#if cityflag}}
  {{#each petitioners}}
    <p>
      {{#if url}}<a href="{{url}}" target="_blank">
       {{first}} {{last}}</a>
      {{else}}
       {{first}} {{last}}
      {{/if}}
      {{age}}
    </p>
  {{/each}}
{{else}}
  {{#each petitioners}}
         <p>
           {{#if url}}<a href="{{url}}" target="_blank">
            {{first}} {{last}}</a>
           {{else}}
            {{first}} {{last}}
           {{/if}}
           {{age}}
           {{#if city}}
            <a href="/petition/signers/{{city}}">{{city}}</a>
           {{/if}}
         </p>
  {{/each}}
{{/if}}
